# v1前の課題管理メモ

このファイルは、v1公開前に確認すべき不具合、仕様ズレ、改善候補、検証済み項目を整理するためのメモです。

## 不具合

- 素材回収が複数回カウントされないことを継続確認する。
  - 回収開始時点で `collected` を立て、物理判定を無効化する方針。
  - 確認: 素材を1つ拾った時、素材数が1だけ増える。

- 危険物接触でHPが減らないケースがないか確認する。
  - 矩形判定だけに頼らず、中心距離でも接触判定する方針。
  - 連続ヒットしすぎないよう、短い無敵時間は維持する。
  - 確認: 危険物に触れるとHPが減る。

- タッチ操作中にブラウザスクロールが起きないか確認する。
  - 仮想スティック、掃除ボタン、回避ボタンを同時操作した時も見る。

- PWAキャッシュが古いファイルを残さないか確認する。
  - 公開前にキャッシュ名とService Worker更新手順を確認する。

## 仕様ズレ

- 旧仕様の「1フロア」「旧プレイ時間」「固定左右操作」「非戦闘寄り表現」が残っていないか確認する。
  - v1ではB1F〜B5F、1回1〜3分、左右反転操作、清掃バトル寄りの危険物対処を基準にする。

- 現在の「攻撃」表記とボタンは、v1方針では正式戦闘ではない。
  - 「追い払い」「片付け」「払う」などの清掃文脈を残しつつ、攻撃力や武器の成長要素と接続する。
  - スマホでボタン数が増えすぎる場合は、状況依存操作または掃除操作への統合を検討する。
  - PCの `J` キーは、当面「清掃道具アクション」として扱う。

- モンスターを倒す報酬に見える表現は避ける。
  - 「倒した」より「追い払った」「片付いた」「素材が残った」という文言を優先する。

- 図鑑、持ち物、依頼、ショップが空のタブに見えないようにする。
  - v1では最低限の一覧、説明、準備中メッセージを用意する。

## 改善候補

- 一時停止メニューから、今回ダンジョン内で持っている素材、バッグ容量、清掃率、HP、所持金を確認できるようにする。
- ダンジョン画面右上にミニマップ風パネルと `広間の清掃 4/5` のような進捗表示を置く。
- ダンジョンをB1F〜B5F構成にし、B5Fを最奥地として扱う。
- 出撃ごとにマップ配置とスタート位置をある程度ランダム化する。
- 各フロアのモンスター数を2〜5体の範囲でランダム化する。
- ダンジョンに行くごと、または深い階へ行くごとにモンスターを少し強くする。
- モンスターの向きが分かる視線扇形を出す。
- モンスターがプレイヤーに気づいたら `!` を出して追いかける。
- 設定から操作UIを左下スティック/右下アクションと右下スティック/左下アクションで反転できるようにする。
- タイトル/ホーム画面に上部リソースバー、今日の清掃メモ、下部タブを追加する。
- 図鑑にモンスター、素材、宝、プレイヤー成長の見た目を整理して表示する。
- 素材ごとに形と色を変え、スマホでも見分けやすくする。
- 危険物の見た目を赤い四角から、警告色、影、揺れ、`!` を持つデザインにする。
- 追い払い時の演出を、斬撃ではなくほうきの弧、粉じん、軽い後退に寄せる。
- 素材でほうきや武器を生産できるUIを追加する。
- 攻撃力、スタミナ、ほうき、バッグの強化導線を分かりやすくする。
- がれきの大きさごとに清掃率の増加量を変える。
  - 小さいがれき: 5%
  - 中くらいのがれき: 10%
  - 大きいがれき: 15%
- 掃除途中で掃除ボタンを離すと、未完了がれきの個別掃除ゲージが少しずつ戻る。

## 現時点の残タスク

- 実機タッチ確認
  - iPhone/Androidで、左右反転後の同時押し、掃除の開始/中断、回避、払う操作、セーフエリアを確認する。
  - 仮想スティック操作中にブラウザスクロールや掃除の誤解除が起きないかを見る。
- PWAキャッシュ確認
  - ホーム画面追加、オフライン起動、Service Worker更新、古いキャッシュの残り方を実機と公開URLで確認する。
  - 公開前にキャッシュ名と更新手順をもう一度確認する。
- 報酬/清掃テンポ調整
  - B1F〜B5Fが1〜3分に収まるよう、移動距離、残骸量、清掃率、出口開放条件を調整する。
  - 売上、強化費用、生産素材数を見直し、リザルト後に「もう一回行く」理由が残るようにする。
- 図鑑画面の専用化
  - 現在の図鑑相当メモを、モンスター、素材、宝、プレイヤー成長の収集画面として独立させる。
  - 収集済み/未収集、素材用途、危険物の特徴がスマホで読める表示にする。
- グラフィック仕上げ
  - タイトル背景、拠点パネル、ダンジョン床/壁、残骸、素材、危険物、出口、リザルト報酬演出を軽量アート制作ルールに沿って磨く。
  - 外部画像の直貼りではなく、Phaser図形描画、生成テクスチャ、軽量なドット絵風パーツで進める。

## 検証済みとして扱う条件

- `npm run build` が成功する。
- タイトルからホーム、ダンジョン、リザルトへ進める。
- 1回1〜3分で出撃が終わる。
- B1F〜B5Fを進行でき、B5Fが最奥地として扱われる。
- 出撃ごとにマップ配置とスタート位置がある程度変わる。
- 各フロアに2〜5体のモンスターが出る。
- モンスターの視線扇形、発見 `!`、追跡が分かりやすい。
- 素材を1つ拾った時、素材数が1だけ増える。
- メニューで今回の素材数が確認できる。
- 危険物接触でHPが減る。
- 清掃道具アクションで危険物を追い払える。
- 素材でほうきや武器を生産できる。
- 小/中/大がれきの清掃完了で清掃率が5/10/15%増える。
- 掃除を中断すると、掃除中だったがれきの進捗が徐々に戻る。
- 右下操作ボタンと左下仮想スティックを同時に使っても、画面スクロールが起きない。
- 操作UIを左右反転した後も、移動とアクションを同時に使える。

## v1公開前チェック

- 旧「副業タイプ診断」由来の広告、問い合わせ、AdSense、ASP運用の文言が残っていない。
- `README.md`、`PLAN.md`、`DESIGN.md`、`BUGS.md`、`AGENTS.md`、`Skills.md` がUTF-8の日本語で読める。
- README、PLAN、AGENTSでプレイ時間が1〜3分に統一されている。
- v1対象外のサーバー、DB、ログイン、ランキング、課金、クラウドセーブ、マルチプレイ、チャット、リアルタイム通信を実装していない。
- Cloudflare Pagesで静的ビルドを公開できる。

## 2026-07-05 Phase 7 / 受け入れテスト結果

確認済み:

- `npm.cmd install` が成功する。
- `npm.cmd run build` が成功する。
- ビルド済み `dist` を静的配信し、タイトルから拠点、ダンジョン B1F まで進める。
- PCサイズで `D`、`S`、`Space`、`J`、`Esc` 操作後もコンソールエラーが出ない。
- スマホ縦画面相当の 390x844 でUIが表示され、`touch-action: none`、`overflow: hidden`、スクロール位置0を確認した。
- ダンジョンHUDに B1F 表示、スマホ操作ボタン、視線扇形、危険物、残骸が表示される。
- PWA用の `manifest.json`、`sw.js`、アイコンが `dist` に出力される。
- Cloudflare Pages向け `pages_build_output_dir = "dist"` を確認した。

実装で補強した項目:

- B1F〜B5Fの階層進行とB5F最奥地表示。
- 出撃ごとの開始位置、残骸配置、危険物数2〜5体のランダム化。
- 危険物の視線扇形、発見時 `!`、追跡挙動。
- 操作UI左右反転設定の保存と、反転後のスティック/ボタン配置。
- 図鑑相当メモ、持ち物、マップ、依頼、ショップの最低限画面。
- ほうき、バッグ、追い払い、スタミナ強化と、素材によるほうき/作業道具生産。
- リザルトで素材を保存し、生産導線につなげる。
- 「攻撃」文言を「払う」「追い払い」「片付け」寄りに調整。
- Service Workerキャッシュ名をv1向けに更新。

残る実機確認:

- 実際のiPhone/Androidで、PWAインストール、ホーム画面起動、オフライン起動、キャッシュ更新を確認する。
- 実機タッチで左右反転後の同時押し、掃除の開始/中断、回避、払う操作を確認する。
- Cloudflare Pagesへデプロイ後、公開URLでタイトル、拠点、ダンジョン、リザルト、PWA登録を確認する。

公開後改善の優先順位:

1. スマホ実機で押しづらいボタン位置、セーフエリア、掃除の誤解除を直す。
2. 清掃完了、素材吸い込み、危険物を払う演出の気持ちよさを上げる。
3. 報酬量、強化費用、生産素材数を調整し、もう一回行きたい感覚を強める。
4. B1F〜B5Fの移動距離と清掃量を見直し、1〜3分に収まる密度へ寄せる。
5. 図鑑表示を専用画面として育て、収集済み/未収集が分かる見た目にする。

## 2026-07-05 掃除トグル化 / 公開前確認

実装済み:

- 掃除ボタン/Spaceを、一度押すと掃除開始、もう一度押すと中断するトグル操作に変更した。
- 掃除対象は開始時にロックし、射程外へ離れたら自動中断する。
- 中断した残骸は、再度掃除ボタンを押すと途中進捗から再開できる。
- ダンジョン端でもプレイヤーが中央付近に映るよう、カメラのマップ端固定を外した。
- Service Workerキャッシュ名を `chacha-v1-20260705-clean-toggle` に更新した。

確認済み:

- `npm.cmd run build` が成功する。
- `dist/sw.js` に新しいキャッシュ名が出力される。
- `dist/manifest.json` にPWA用の名前、縦画面指定、アイコン指定が出力される。

未完了/ブロック:

- ブラウザ自動確認は、ローカルURL接続がブラウザ実行環境側で拒否され完了できなかった。
- Cloudflare Pagesデプロイは、`CLOUDFLARE_API_TOKEN` 未設定のため `wrangler pages deploy dist --project-name chacha` が停止した。
- 実機iPhone/Androidでのホーム画面追加、オフライン起動、左右反転後の同時押し、セーフエリア確認は未実施。
